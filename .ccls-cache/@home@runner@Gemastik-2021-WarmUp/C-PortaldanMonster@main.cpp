#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;
// g++ main.cpp -o main && ./main
int main() {
  int n, k;
  cin >> n >> k;
  int m[n], a[n], b[n];
  for (int x = 0; x < n; x++)
    cin >> m[x];
  for (int x = 0; x < k; x++)
    cin >> a[x] >> b[x];
}